import { findByLabelText } from '@testing-library/dom';
import Colors from './Colors';

const metrics = {
    navBarElem: {
        listStyle: 'none',
        display: 'flex-row',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: Colors.mainGold,
        color: "black",
        padding: '2%', 
        fontSize: 30, 
        cursor: PointerEvent,
    }
    
}

export default metrics; 